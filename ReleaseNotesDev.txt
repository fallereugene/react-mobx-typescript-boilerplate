Изменения:

[!] Важное замечание
[+] Нововведение
[-] Исправлена ошибка
[*] Изменения
[nn] Номер зарегистрированной ошибки или задачи

[*] Рефакторинг invokeApi
[+] Добавлена возможность генераций форм через дескрипторы. Компонент DynamicFormGenerator.
[+] Добавлен пакет mobx-logger для чтения данных в консоли
[+] Расширены нотификации mobx
[*] Обновлены пакеты в рамках аудита.

[] Известные проблемы:
1. если получить стор через хук, то при работе RHL возвращается пустой объект:
import { StoreContext } from '@/index';
const useStore = () => React.useContext(StoreContext);

Вопрос решен получением контекста непосредственно в компоненте: React.useContext(StoreContext)
Однако подобная проблема наблюдалась и через найденное решение, например, если RequerstErrors сделать обсервером за ошибками.
Эти проблемы только при hot reload-е. Пока все решено.

Хук на всякий случай оставил.

2. Не всегда работает ререндер при push на массивах. Вопрос решен через [...this.requestErrors, xCorrelationId]
