import { Interpolation, Theme } from '@mui/material/styles';

export const globalStyles: Interpolation<Theme> = {
    a: {
        textDecoration: 'none',
        color: 'inherit',
    },
    '#root': {
        padding: '0 15px',
    },
};
